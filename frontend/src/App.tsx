/**
 * Placeholder root component for Tier 0's scaffolding smoke test.
 * Real screens (Screen A-D, D1) are built in Tier 2, Task 2B (§5) — this
 * file exists only to prove the Vite+React+TS+Tailwind pipeline boots.
 */
function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-white text-slate-900">
      <h1 className="text-2xl font-semibold">LitList</h1>
    </main>
  )
}

export default App
